# Python dependencies for AI-Powered Security Analysis Pipeline

# Core dependencies
requests>=2.31.0
python-dateutil>=2.8.2

# Optional: For enhanced JSON processing
jsonschema>=4.19.0

# Optional: For better HTML rendering (if using Jinja2 templates in future)
jinja2>=3.1.2

# Note: No additional ML libraries needed as we use HuggingFace Inference API
# which handles all model inference remotely
